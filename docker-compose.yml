version: "3.9"

services:
  forum:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/forum.db
    volumes:
      - forum_data:/data

volumes:
  forum_data:
